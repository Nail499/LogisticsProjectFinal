package com.ltc.logisticsproject.entity;

// Reys öncəsi/sonrası yoxlama siyahısı (DVIR — Driver Vehicle Inspection
// Report) — bax DvirInspection, DriverController#submitDvir.
public enum DvirType {
    PRE_TRIP,
    POST_TRIP
}
